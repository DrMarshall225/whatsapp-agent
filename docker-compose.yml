services:
  whatsapp-agent:
    build:
      context: .
    container_name: whatsapp-agent
    restart: unless-stopped
    environment:
      WAHA_PRINT_QR: "false"

      PORT: "3000"

      DB_HOST: "65.109.27.58"
      DB_PORT: "55432"
      DB_USER: "postgres"
      DB_PASSWORD: "ayYtneAT6CuKks"
      DB_NAME: "whatsapp_agent"

      # Appeler WAHA via le réseau docker (service name = waha)
      WAHA_BASE_URL: "http://waha:3000"
      WAHA_API_KEY: "BekoWahaKey_2025_Abidjan_9xPqL7vT2"
     
      # URL n8n
      N8N_COMMAND_BOT_URL: "https://n8n.srv853938.hstgr.cloud/webhook/commandBot"

    
    ports:
      - "3002:3000/tcp"
    
 
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=root_default"
      - "traefik.http.routers.api-maboutiki.rule=Host(`api.maboutiki.com`)"
      - "traefik.http.routers.api-maboutiki.tls=true"
      - "traefik.http.routers.api-maboutiki.entrypoints=web,websecure"
      - "traefik.http.routers.api-maboutiki.tls.certresolver=mytlschallenge"
      - "traefik.http.services.api-maboutiki.loadbalancer.server.port=3000"
      # CORS headers
      - "traefik.http.middlewares.api-cors.headers.accesscontrolallowmethods=GET,POST,PUT,DELETE,OPTIONS"
      - "traefik.http.middlewares.api-cors.headers.accesscontrolalloworiginlist=*"
      - "traefik.http.middlewares.api-cors.headers.accesscontrolallowheaders=Content-Type,Authorization"
      - "traefik.http.middlewares.api-cors.headers.accesscontrolmaxage=100"
      - "traefik.http.middlewares.api-cors.headers.addvaryheader=true"
      - "traefik.http.routers.api-maboutiki.middlewares=api-cors@docker"

networks:
  appnet:
    external: true
    name: appnet
  root_default:
    external: true
    name: root_default